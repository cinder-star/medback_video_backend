# MedSocket

## How to

- Installing dependencies

  With yarn:

  ```bash
  yarn
  ```

  Or with npm:

  ```bash
  npm install
  ```

- Running the code

  With yarn:

  ```bash
  yarn dev
  ```

  Or with npm:

  ```bash
  npm dev
  ```
